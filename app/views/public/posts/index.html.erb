<h1>投稿一覧</h1>
<%= link_to "投稿する",new_public_post,class: "btn" %>
<div class="row">
  <% @posts.each do |post| %>
    <%= link_to post.user.name,public_user_path(post.user.id),class: "#" %>
    <%= post.body,class: "#" %>
  <% end %>
</div>