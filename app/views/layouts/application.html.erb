<!DOCTYPE html>
<html>
  <head>
    <title>Gameconnect</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <!-- drawer.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.2.1/css/drawer.min.css">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/iScroll/5.1.3/iscroll.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.1.0/js/drawer.min.js"></script>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
  </head>

  <body class="drawer drawer--left h-100 w-100">
    <header role="banner">
      <div class="container-fluid bg-dark">
        <div class="row d-flex">
          <nav class="navbar navbar-expand-lg navbar-dark bg-dark d-flex">
            <button class="navbar-toggler p-0 border-0" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse offcanvase-collapse open" id="navbarNavDropdown">
              <ul class="navbar-nav flex-column ml-auto">
                <% if user_signed_in? %>
                  <li class="nav-item ">
                    <%= link_to destroy_user_session_path,method: :delete,class: "nav-link text-light" do %>
                      <i class="fas fa-right-from-bracket"></i> ログアウト
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to public_posts_path,class: "nav-link text-light" do %>
                      <i class="fas fa-book"></i> 投稿一覧
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to new_public_post_path,class: "nav-link text-light" do %>
                      <i class="fas fa-pen"></i> 投稿する
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to public_user_path(current_user),class: "nav-link text-light" do %>
                      <i class="fas fa-house"></i> マイページ
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to favorites_public_user_path(current_user),class: "nav-link text-light" do %>
                      <i class="fas fa-heart"></i> お気に入り
                    <% end %>
                  </li>
                <% elsif admin_signed_in? %>
                  <li class="nav-item">
                    <%= link_to admin_users_path,class: "nav-link text-light" do %>
                      <i class="fas fa-users"></i> ユーザー一覧
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to admin_posts_path,class: "nav-link text-light" do %>
                      <i class="fas fa-book"></i> 投稿一覧
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to admin_comments_path,class: "nav-link text-light" do %>
                      <i class="fas fa-comments"></i> コメント一覧
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to destroy_admin_session_path,method: :delete,class: "nav-link text-light" do %>
                      <i class="fas fa-right-from-bracket"></i> 管理者ログアウト
                    <% end %>
                  </li>
                <% else %>
                  <li class="nav-item">
                    <%= link_to new_user_session_path,class: "nav-link text-light" do %>
                      <i class="fas fa-right-to-bracket"></i> ログイン
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to users_guest_sign_in_path, method: :post,class: "nav-link text-light" do %>
                      <i class="fas fa-user"></i> ゲストログイン(閲覧用)
                    <% end %>
                  </li>
                  <li class="nav-item">
                    <%= link_to new_admin_session_path,class: "nav-link text-light" do %>
                      <i class="fas fa-user-tie"></i> 管理者ログイン
                    <% end %>
                  </li>
                <% end %>
              </ul>
            </div>
          </nav>
            <a class="logo p-2 m-2 form-inline" href="/">title</a>
              <div class="form-inline flex-row ml-auto">
                <% if user_signed_in? %>
                  <%= render 'public/shared/search'%>
                  <%= link_to public_notifications_path,class: "nav-link text-secondary ml-auto mr-auto" do %>
                  <i class="fas fa-bell fa-2x"></i><% end %>
                <% elsif admin_signed_in? %>
                  <%= render 'admin/shared/search' %>
                <% end %>
              </div>
        </div>
      </div>
    </header>

    <div class="container-fluid row">
        <main class="bg-light w-100">
          <div class="flash">
            <% if flash[:notice] %>
              <p class="text-success text-center"><%= notice %></p>
            <% elsif flash[:alert] %>
              <p class="text-danger text-center"><%= alert %></p>
            <% end %>
          </div>
          <%= yield %>
        </main>
      </div>
  </body>
</html>